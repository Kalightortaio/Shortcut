If you are updating this module:

1. Transfer all of the Shortcut.zip contents into the "$($env:ProgramFiles)\WindowsPowerShell\Modules\Shortcut\Update" folder. (Unzip it!).
 
2. Use the command "shortcut update" to complete the process.

3. Use the command "shortcut reload" to reload the module in an existing window.
