#Configuration File for the shortcut module

#Change the number from zero to literally anything else to successfully install the shortcut module.
$ValueMustNotBeZero = 0

#Change the drive letter to match what corresponds with the Windows Boot Drive. Default is set to "C"
$BootDrive = "C"
