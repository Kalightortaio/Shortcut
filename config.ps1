#Configuration File for the shortcut module

#Change letter to the default drive, for example I had multiple hard drives so I used D.
#This will be used for hardcoded access to your documents, downloads, and desktop.
$DefaultDrive = "C"

#Change letter to the Windows boot drive, for example I used C. This will be used for the installation process and other misc. functions.
$BootDrive = "C"
